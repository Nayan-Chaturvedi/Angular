import { NumberPipesPipe } from './number-pipes.pipe';

describe('NumberPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
